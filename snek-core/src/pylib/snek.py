from snek_core import add_two_internal

# TODO: this is not being used anywhere, but it could be imported in demo.py using 'from snek import add_two'
def add_two(num):
    return add_two_internal(num)
